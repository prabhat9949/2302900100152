# API Integration

Authentication:

Bearer Token

APIs Used:

1. GET /depots
2. GET /vehicles
3. GET /notifications

The Authorization header is used:

Authorization: Bearer <token>

The data received from these APIs is used for:

- Vehicle Maintenance Scheduler
- Notification Priority Inbox